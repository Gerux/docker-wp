version: "3.4"
services:
  nginx:
    image: nginx:latest
    container_name: "nginx"
    userns_mode: "host" # What is it?
    ports:
      - "80:80"
    links:
      - php
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./app:/var/www/html # why ro?
      - ./log/nginx:/var/log/nginx
  php:
    build:
      context: ./docker/php
    userns_mode: "host"
    container_name: "dmp_php"
    user: "1000:1000"
    ports:
      - "9000"
    expose:
      - 9003 # What is it?
    extra_hosts:
      - "host.docker.internal:host-gateway" # What is it?
    links:
      - mysql # What is it?
    volumes:
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/docker-php.ini
      - ./app:/var/www/html

  mysql:
    image: mysql:latest
    userns_mode: "host"
    container_name: "dmp_mysql"
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    volumes:
      - ./db/mysql:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=dmp
      - MYSQL_DATABASE=dmp
      - MYSQL_USER=dmp
      - MYSQL_PASSWORD=dmp
      - TERM=meh